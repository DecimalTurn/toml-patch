/**
 * @deprecated Use "./index" (package root export) instead.
 *
 * Compatibility shim kept so dist/toml-patch.js continues to exist
 * for tooling or scripts that still reference the legacy path.
 */
export * from './index';
