# Maximum allowed slowdown of the current toml-patch build relative to
# smol-toml, measured in the same benchmark run. The CI benchmark job fails
# when toml-patch is more than `maxSlowdown` times slower than smol-toml on
# any fixture for that operation.

[parse]
maxSlowdown = 10

[stringify]
maxSlowdown = 40
